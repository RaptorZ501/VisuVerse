{% extends 'base.html.twig' %}

{% block title %}Onglet{% endblock %}

{% block body %}
    <h1>Onglet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ onglet.id }}</td>
            </tr>
            <tr>
                <th>Title_onglet</th>
                <td>{{ onglet.TitleOnglet }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ onglet.img }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ onglet.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ onglet.description }}</td>
            </tr>
            <tr>
                <th>Liens</th>
                <td>{{ onglet.Liens }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_onglet_index') }}">back to list</a>

    <a href="{{ path('app_onglet_edit', {'id': onglet.id}) }}">edit</a>

    {{ include('onglet/_delete_form.html.twig') }}
{% endblock %}
