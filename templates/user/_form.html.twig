{{ form_start(form, {'attr': {'class': 'formEdit'}})}}
        <!-- pseudo -->
        <div class="form-group mb-3">
            {{ form_label(form.pseudo, 'Pseudo', {'attr': {'for': 'pseudo', 'class': 'form-label'}}) }}
            {{ form_widget(form.pseudo, {'attr': {'id': 'pseudo', 'class': 'form-control'}}) }}
            {{ form_errors(form.pseudo) }}
        </div>

        <!-- mail -->
        <div class="form-group mb-3">
            {{ form_label(form.mail, 'E-mail', {'attr': {'for': 'pseudo', 'class': 'form-label'}}) }}
            {{ form_widget(form.mail, {'attr': {'id': 'pseudo', 'class': 'form-control'}}) }}
            {{ form_errors(form.mail) }}
        </div>

        <!-- code_postal -->
        <div class="form-group mb-3">
            {{ form_label(form.code_postal, 'Code postal', {'attr': {'for': 'pseudo', 'class': 'form-label'}}) }}
            {{ form_widget(form.code_postal, {'attr': {'id': 'pseudo', 'class': 'form-control'}}) }}
            {{ form_errors(form.code_postal) }}
        </div>

        <!-- lastName -->
        <div class="form-group mb-3">
            {{ form_label(form.lastName, 'Nom', {'attr': {'for': 'pseudo', 'class': 'form-label'}}) }}
            {{ form_widget(form.lastName, {'attr': {'id': 'pseudo', 'class': 'form-control'}}) }}
            {{ form_errors(form.lastName) }}
        </div>

        <!-- firstName -->
        <div class="form-group mb-3">
            {{ form_label(form.firstName, 'Prenom', {'attr': {'for': 'pseudo', 'class': 'form-label'}}) }}
            {{ form_widget(form.firstName, {'attr': {'id': 'pseudo', 'class': 'form-control'}}) }}
            {{ form_errors(form.firstName) }}
        </div>

            <a href="" class="editPass" hidden>Changer de mot de passe</a>
            <!-- Validation -->
            <button class="btn btn-warning editMod" type="submit">{{ button_label|default('Valider') }}</button>

{{ form_end(form) }}
