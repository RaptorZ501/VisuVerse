{% extends 'base.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}
    {% include '/reusable/header.html.twig'%}
    <main class="center">
        <div class="blockEdit center column-flex">
            <h1 class="titleEdit">Modifier le profil</h1>
            {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
            <div class="betweenEdit">
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_user_index') }}" class="btn btn-primary">retour a la liste des membre</a>
                {% else %}
                    <a href="{{ path('app_index') }}" class="btn btn-primary">retour a la page d'acceuil</a>
                {% endif %}
                {{ include('user/_delete_form.html.twig') }}
            </div>
        </div>
    </main>
{% endblock %}
