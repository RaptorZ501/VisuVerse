<form method="post" action="{{ path('app_page_project_delete', {'id': page_project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page_project.id) }}">
    <button class="btn">Delete</button>
</form>
